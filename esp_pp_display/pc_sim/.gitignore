# Per-host build trees + FetchContent caches. Two of them so Windows-side
# (build-windows/) and WSL-side (build-linux/) builds don't fight over a
# shared CMakeCache.txt.
build-windows/
build-linux/

# Local sim state -- nvs.json + sdcard/* mirror written by mock_nvs and
# the redirected fopen path. Per-developer; never commit.
pc_sim_state/
